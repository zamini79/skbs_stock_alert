# 프로젝트: SK바이오사이언스(302440) 주가 변동 자동 보고

## 목적
코스피 상장사 **SK바이오사이언스(종목코드 302440)** 의 주가가 전일 대비 ±5% 이상 움직이면,
그 원인을 공시·뉴스·시장 맥락을 근거로 자동 분석해 텔레그램 방으로 보고하는 IR 자동화 도구다.
회사 IR팀이 사장님께 드리던 "주가 ±5% 변동 시 원인 보고"를 자동화하는 것이 목표.

## 환경 (중요)
- **macOS / zsh** 환경이다. 명령어·경로·환경설정 안내는 모두 macOS/zsh 기준으로 작성할 것. (PowerShell 금지)
- Python 3. 외부 의존성은 `requests` 하나뿐 (`pip install -r requirements.txt`).
- API 키는 **코드에 하드코딩하지 않는다.** 모두 환경변수로 읽는다 (`.env.example` 참고, `~/.zshrc`에 export).

## 실행 / 테스트
```zsh
pip install -r requirements.txt
python3 stock_alert_302440.py          # 1회 실행 (현재가 확인 → 조건 충족 시 보고)
```
- 등락률이 임계치 미만이면 아무것도 보내지 않고 종료한다.
- 하루 1회만 보고하도록 `~/.stock_alert_302440_state.json`에 마지막 보고 날짜를 기록한다(중복 방지).

## 파이프라인 구조 (stock_alert_302440.py)
1. **감지** — `kis_token()` → `get_price()` : 한국투자증권(KIS) API로 현재가·전일대비 등락률 조회
2. **트리거** — `abs(change_rate) >= THRESHOLD(5.0)` 이고 오늘 미보고일 때만 진행
3. **원인 수집**
   - `get_disclosures()` : OpenDART 당일 공시 목록
   - `get_news()` : 네이버 뉴스 검색 당일 헤드라인
   - `get_kospi()` : 코스피 지수 등락률(시장 전반 영향인지 판별용)
4. **분석** — `analyze()` : Claude API에 재료를 묶어 보고서 생성
5. **전송** — `send_telegram()` : 텔레그램 봇으로 지정 방에 전송

## 구현 세부 (코드를 읽어야 알 수 있는 것 — stock_alert_302440.py)
- **KIS `tr_id`/엔드포인트 매핑** (변경 시 깨지기 쉬움):
  - `get_price()` → `tr_id=FHKST01010100`, `inquire-price`, 시장구분 `J`, 응답 필드 `stck_prpr`(현재가)·`prdy_ctrt`(등락률)·`acml_vol`(거래량).
  - `get_kospi()` → `tr_id=FHPUP02100000`, `inquire-index-price`, 시장구분 `U`, 코드 `0001`, 필드 `bstp_nmix_prdy_ctrt`.
- **KIS_BASE는 실전투자 도메인**(`openapi.koreainvestment.com:9443`). 모의투자 키를 쓰면 동작하지 않음.
- **환경변수 미설정 시 fail-fast 안 함**: 모든 키가 `여기에_...` 한글 플레이스홀더 문자열로 폴백한다.
  키가 비면 에러 없이 잘못된 요청을 보내므로, 디버깅 시 인증 실패/이상 응답을 먼저 의심할 것.
- **예외 처리는 `get_kospi()`에만 있음**: `get_disclosures()`·`get_news()`·`analyze()`·`send_telegram()`·`main()`
  전체가 `try/except` 없이 `raise_for_status()`에 의존 → 보조 데이터 단계 실패도 전체 실행을 중단시킨다.
  (TODO #3 "로깅·에러 처리 강화"의 직접적 동기. 컨벤션상 보조 수집은 실패해도 안전해야 하나 현재 미준수.)
- **상태 파일 형식**: `~/.stock_alert_302440_state.json` = `{"date": "YYYY-MM-DD"}`. 날짜 문자열 비교로 하루 1회 보고를 판정.
- **현재가 로그(`print`)는 임계치와 무관하게 매 실행 출력** → cron으로 5분마다 돌려도 로그로 시세 추적 가능.
- `CLAUDE_MODEL`은 `claude-sonnet-4-6`으로 하드코딩(58행). 모델 교체 시 이 상수만 수정.

## 코드 컨벤션
- 주석·로그·보고서 출력은 **한국어**.
- 새 함수는 단일 책임으로, `main()`의 파이프라인 순서를 따른다.
- 외부 호출에는 `timeout`을 반드시 지정하고 실패 시 안전하게 처리(특히 보조 데이터 수집).
- **분석 프롬프트는 바이오·제약 섹터 맥락을 유지한다**: 임상 결과, 품목허가, 기술수출/공급계약,
  식약처·FDA 이슈, 모회사 SK케미칼 관련 등. 코스피가 같은 방향으로 크게 움직였으면 '개별 이슈'가
  아닌 '시장 전반 영향' 가능성을 명시하게 할 것.

## API 키 (모두 무료 발급, 환경변수명)
- `KIS_APP_KEY`, `KIS_APP_SECRET` — 한국투자증권 KIS Developers (계좌 필요)
- `DART_API_KEY` — OpenDART 인증키
- `DART_CORP_CODE` — SK바이오사이언스의 **DART 고유번호(8자리)**. 종목코드(302440)와 다름.
  OpenDART `corpCode.xml` 다운로드 API로 1회 조회해 채워야 함. (아직 미설정 — TODO 참고)
- `NAVER_CLIENT_ID`, `NAVER_CLIENT_SECRET` — 네이버 검색 API
- `TELEGRAM_TOKEN`, `TELEGRAM_CHAT_ID` — @BotFather 봇 토큰 + 대상 방 chat_id
- `ANTHROPIC_API_KEY` — Claude API

## 현재 상태 (완료)
- 4단계 전체 흐름이 한 파일에 구현됨.
- 환경변수 기반 설정, 중복 알림 방지(쿨다운) 적용.

## 남은 작업 (TODO — 우선순위 순)
1. **DART 고유번호 조회 헬퍼 추가**: `corpCode.xml`을 받아 SK바이오사이언스의 `corp_code`를
   찾아 출력/저장하는 일회성 스크립트(`tools/find_corp_code.py`)를 만든다. 현재 `DART_CORP_CODE`가
   비어 있으면 공시 단계가 동작하지 않는다.
2. **검수용 라우팅**: 현재는 텔레그램 방에 직접 전송한다. AI 원인 분석은 추정이므로,
   사장님 방으로 직행하기 전 IR 담당자 검수 단계(예: 담당자 방으로 먼저 보내고 승인 시 전달)를
   넣는 옵션을 고려한다.
3. **로깅·에러 처리 강화**: 단계별 실패를 파일 로그로 남기고, 핵심 단계 실패 시 관리자에게 알림.
4. **스케줄링**: 장중(평일 09:00~15:30) 5분 간격 실행. macOS는 cron/launchd 가능하나 절전 시
   멈추므로, 24시간 안정 운영이 필요하면 클라우드(예: 작은 서버 또는 서버리스)로 이전 검토.
5. **뉴스 단계 보강**: 바이오는 공시보다 기사가 먼저 뜨는 경우가 많음. 검색어 다양화(종목명+이슈),
   중복 제거, 시각 필터 등 정확도 개선.
6. (선택) **n8n 포팅**: 노코드 유지보수가 필요하면 동일 흐름을 n8n 워크플로로 옮긴다.

## 주의사항
- 상장사 IR 자료다. **AI 분석은 추정**임을 보고서에 항상 명시(코드의 프롬프트에 반영돼 있음).
- KIS API는 실전/모의 키가 다르고 호출 한도(rate limit)가 있으니 폴링 간격에 유의.
- 키를 절대 코드·로그·커밋에 노출하지 말 것. `.env`와 상태 파일은 `.gitignore`로 제외돼 있음.
